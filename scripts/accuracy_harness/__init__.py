"""Accuracy harness — synthetic ground-truth verifiers for ClawMetry features.

Tokens-first proof-of-concept (PR #TBD). The shape established here
(`drive ground truth → wait for flush → query every window → diff →
report or file drift issue`) is intended to extend to approvals and
alerts in follow-up iterations.
"""
