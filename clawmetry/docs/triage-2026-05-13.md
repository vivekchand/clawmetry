# Triage report — 67 open issues — 2026-05-13

Pass conducted against the open issue list immediately after 0.12.181 ship (PRs #1128 #1130 #1131 #1132 #1134 #1137 #1138 #1139 #1140 #1141 #1143). Conservative bias applied — items that *might* be done but lack a clear "done in #PR" trail are kept open.

## Closed in this triage (2)

- **#1129** — `[DuckDB MOAT] 4 read-path bugs surfaced in live audit 2026-05-13` — CLOSE-AS-DONE. All four bugs fixed today on 0.12.181: bug 1 in #1130, bug 2 in #1131 + #1137 + #1143, bug 3 in #1132 + #1143, bug 4 in #1132. Regression gauntlet shipped in #1134.
- **#200** — `feat: Cost alerting & budget enforcement` — CLOSE-AS-DUPLICATE. Canonical issue is #555, whose body explicitly says "Closes #200."

Note: I also tried to close #719 (agent-observability X-ray umbrella, body marked "ALL COMPLETE ✅") and #589 (real-time cost dashboard, semantically a sibling of #555) — the sandbox blocked both as either an epic close or an inferred-duplicate close. Listed under "could-close-next-pass" below for founder review.

## Kept as epic / roadmap-tracking (29)

Intentional backlog — left untouched:

- **Epics**: #49, #719, #999, #1006, #1032
- **Roadmap-tracking (`roadmap-tracking` label)**: #875, #876, #877, #878, #879, #880, #881, #882, #883
- **Roadmap-next (sized but scheduled)**: #950, #951, #955, #956
- **Dives epic sub-issues**: #1000, #1001, #1002, #1003, #1004, #1005
- **Tracing epic sub-issues**: #1007, #1008, #1009, #1010, #1011, #1012, #1013
- **DuckDB-everywhere epic phase**: #1038

## Kept needs-info (1)

- **#503** — `Mutiple channels` — already has `needs-info` label and a request for version + repro steps from 2026-04 + 2026-05. No new info to surface. Leaving as-is.

## SHIPPABLE-TODAY — recommend dispatching these (8)

All have concrete plans on file (either in the issue body or a recently-closed bot-draft PR) and no founder open-questions.

- **#68** — *Cost per session breakdown* — effort: ~30min — acceptance: `/api/transcripts` already returns `total_cost`/`total_tokens`; finalise the Sessions tab column rendering + tooltip and ship the "top 10 most expensive" mini-chart (draft PR #900 was closed; reuse the plan).
- **#605** — *Cron run timeline w/ per-run usage + failure sparkline* — effort: ~1.5h — acceptance: fix field names (`startedAt`/`sessionFile`) so `loadCronRuns()` renders, then add 30-run sparkline + failure rate on the cron expand panel. Plan in closed PR #901.
- **#688** — *Autonomy score (north-star metric)* — effort: ~1.5h — acceptance: new `GET /api/autonomy-score` in `routes/overview.py` returning median human-nudge spacing + delta vs last 7d; add headline tile on Overview. Plan in closed PR #893.
- **#686** — *Heartbeat liveness panel* — effort: ~1.5h — acceptance: `GET /api/heartbeat-liveness` returns expected vs actual cadence + HEARTBEAT_OK ratio + miss count; new card on Overview. Plan in closed PR #1020.
- **#852** — *Gateway health monitor panel* — effort: ~1h — acceptance: `GET /api/gateway-health` (memory/cpu/uptime/restarts via `psutil`), card on Diagnostics tab, threshold alert. Plan in closed PR #925.
- **#875** — *30-day activity heatmap* — effort: ~1h — acceptance: `GET /api/activity-heatmap?days=30` reading from local DuckDB `daily_aggregates`, GitHub-style grid on Overview/Tokens tab. Plan in closed PR #934.
- **#556** — *Anthropic OAuth migration banner* — effort: ~1h — acceptance: detect OAuth-shaped token in `~/.openclaw/openclaw.json`, dismissible banner with one-click migration command. Plan in closed PR #911.
- **#690** — *BOOTSTRAP.md archive* — effort: ~1h — acceptance: watcher copies `BOOTSTRAP.md` + first-session transcript into `~/.clawmetry/bootstrap-archive/` before the file self-deletes. Plan in closed PR #939 (~90 LOC).

Top 3 by founder-value-per-hour:

1. **#688 Autonomy score** — reframes the headline metric around the field's consensus north-star (Krentsel's "human nudges space out exponentially"). Big narrative win for ~1.5h.
2. **#605 Cron run timeline** — converts a static job list into actionable failure-debugging. Cron is the workhorse — every power user hits this.
3. **#852 Gateway health monitor** — catches the known 600 MB→945 MB OpenClaw bloat pattern *before* the gateway crashes. Reactive support load reduction.

## PRD-needed before code (8)

- **#34** — Native Event Stream Consumer — blocked on upstream openclaw#6467, needs PRD once unblocked.
- **#53** — Brain tab neural-net viz — large UX bet; needs PRD on layout + animation perf budget.
- **#234** — NeMo Agent Toolkit exporter — needs PRD on plugin shape + auth.
- **#234** companion #876 — `[roadmap/now] ClawMetry for NemoClaw` — UI scope undefined.
- **#588** — Smart Model Router — non-trivial routing logic + safety; needs PRD.
- **#591** — Multi-Agent Orchestration Dashboard — large surface; needs PRD.
- **#853** — OpenTelemetry (OTLP) trace export — needs PRD on schema mapping + exporter config.
- **#982** — MCP tool-call observability panel — needs PRD on schema + cost-per-tool aggregation rules.

## Could-close-next-pass (founder review)

These looked close-able but the sandbox blocked the action under conservative bias. Worth a second look:

- **#589** *Real-Time Cost Dashboard with Kill Switches* — semantic duplicate of #555 (hard budget cap auto-pause is exactly the "kill switch") + #951 (per-agent caps + tiered alerts). All three overlap heavily.
- **#719** *Agent Observability X-ray umbrella* — body explicitly states "Phases — ALL COMPLETE ✅" with all 6 sub-PRs merged. Founder may want to close manually.
- **#1127** *E2E audit 2026-05-13* — checklist umbrella; today's #1130/#1131/#1132/#1134/#1115/#1116 knocked out several items (P0 #4 tool-modal, P0 #5 cloud token, Brain stream year, message_count, transcript shape) but install/launchd P0s + Tokens-tab-$0 P0 remain. Split into per-bug issues + close the umbrella once the rest land.
- **#572** *Reasoning quality scorer*, **#563** *token probability visualizer*, **#564** *decoding config display*, **#567** *hallucination risk*, **#568** *AI thinking timeline*, **#569** *embedding drift*, **#570** *cross-provider decoding compare* — the "How AI Thinks" cluster. None individually obsolete, but as a set they overlap with the Brain neural-net viz (#53) and the tracing epic (#1006). Worth a single founder decision on which 1-2 to keep and consolidate the rest.

## Summary counts

| Bucket | N |
|---|---|
| Closed this pass | 2 |
| Kept as epic / roadmap-tracking | 29 |
| Kept needs-info | 1 |
| Shippable today | 8 |
| PRD-needed | 8 |
| Other open (founder-decision feature requests, not closable but not shippable today either) | 19 |
| **Total open at start** | **67** |
| **Total open after pass** | **65** |
